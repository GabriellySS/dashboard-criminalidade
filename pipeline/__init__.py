# Pipeline package — Strategy Pattern para scrapers multi-estado.
# Estrutura:
#   pipeline/core/         → interfaces abstratas (BaseScraper)
#   pipeline/adapters/     → implementações concretas por estado
