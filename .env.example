# =============================================================================
# VARIÁVEIS DE AMBIENTE — Dashboard Criminalidade
# =============================================================================
# Copie este arquivo para ".env" e preencha com os valores reais.
# NUNCA commite o arquivo ".env" com credenciais reais.
# =============================================================================

# --- Banco de Dados (PostgreSQL) ---
# Formato: postgresql://<usuario>:<senha>@<host>:<porta>/<nome_do_banco>
DATABASE_URL=postgresql://seu_usuario:sua_senha@localhost:5432/ssp_dashboard

# --- API: Política de CORS ---
# Lista de origens permitidas, separadas por vírgula.
# Em desenvolvimento, use http://localhost:5173 (Vite dev server).
# Em produção, liste APENAS os domínios confiáveis da sua aplicação.
# Exemplo produção: CORS_ORIGINS=https://dashboard.seudominio.com.br
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# --- Ambiente da aplicação ---
# "development" | "production"
APP_ENV=development

# --- Cache (Redis) ---
# Formato: redis://<host>:<porta>/<db_index>
# Use redis://localhost:6379/0 para desenvolvimento local (docker-compose up redis).
# Em produção, aponte para o host do Redis gerenciado (ex: Upstash, ElastiCache, etc.)
REDIS_URL=redis://localhost:6379/0

# TTL do cache por rota (em segundos). Opcional — os valores abaixo são os defaults.
# CACHE_TTL_OCORRENCIAS=1800   # 30 minutos
# CACHE_TTL_MUNICIPIOS=86400   # 24 horas
# CACHE_TTL_ESTADOS=86400      # 24 horas
